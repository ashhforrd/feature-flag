# Config Service

Phase 1 will expose APIs for creating, updating, listing, and evaluating flags.

The service should eventually persist flags in PostgreSQL, but the first implementation can use an in-memory repository while the evaluation behavior is being shaped and tested.

